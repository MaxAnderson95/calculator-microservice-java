server.port=${PORT:8080}
spring.application.name=multiply-service

# Logging
logging.level.root=INFO
logging.level.tech.maxanderson.calculator.multiply=DEBUG

# Chaos Engineering
chaos.enabled=${CHAOS_ENABLED:false}
chaos.percent=${CHAOS_PERCENT:#{null}}
