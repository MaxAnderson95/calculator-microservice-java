{{- if .Values.services.divide.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "calculator-microservice-java.fullname" . }}-divide
  labels:
    {{- include "calculator-microservice-java.labels" . | nindent 4 }}
    app.kubernetes.io/component: divide
data:
  PORT: {{ .Values.services.divide.service.port | quote }}

  # Chaos engineering
  {{- if ne .Values.services.divide.chaos.enabled nil }}
  CHAOS_ENABLED: {{ .Values.services.divide.chaos.enabled | quote }}
  {{- else }}
  CHAOS_ENABLED: {{ .Values.global.chaos.enabled | quote }}
  {{- end }}
  {{- $chaosPercent := "" }}
  {{- if ne .Values.services.divide.chaos.percent nil }}
    {{- $chaosPercent = .Values.services.divide.chaos.percent }}
  {{- else if .Values.global.chaos.percent }}
    {{- $chaosPercent = .Values.global.chaos.percent }}
  {{- end }}
  {{- if $chaosPercent }}
  CHAOS_PERCENT: {{ $chaosPercent | quote }}
  {{- end }}
{{- end }}
